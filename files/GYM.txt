<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>IRONFORGE GYM — Forge Your Limits</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow:wght@300;400;500;600;700&family=Barlow+Condensed:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --accent:   #e8ff00;
      --accent2:  #ff4040;
      --bg:       #0a0a0a;
      --bg2:      #111111;
      --bg3:      #181818;
      --surface:  #1e1e1e;
      --border:   #2a2a2a;
      --text:     #f0f0f0;
      --muted:    #888;
      --radius:   4px;
      --transition: 0.35s cubic-bezier(.4,0,.2,1);
    }
    [data-theme="light"] {
      --bg:       #f5f3ef;
      --bg2:      #eeebe5;
      --bg3:      #e5e1d8;
      --surface:  #fff;
      --border:   #d0ccc4;
      --text:     #111;
      --muted:    #777;
    }
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      font-family: 'Barlow', sans-serif;
      background: var(--bg);
      color: var(--text);
      transition: background var(--transition), color var(--transition);
      overflow-x: hidden;
    }
    a { color: inherit; text-decoration: none; }
    ::-webkit-scrollbar { width: 6px; }
    ::-webkit-scrollbar-track { background: var(--bg); }
    ::-webkit-scrollbar-thumb { background: var(--accent); border-radius: 3px; }

    /* NAV */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 5vw; height: 64px;
      background: transparent;
      transition: background .4s, backdrop-filter .4s, box-shadow .4s;
    }
    nav.scrolled {
      background: color-mix(in srgb, var(--bg) 88%, transparent);
      backdrop-filter: blur(20px);
      box-shadow: 0 1px 0 var(--border);
    }
    .nav-logo {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 1.9rem; letter-spacing: .08em; color: var(--accent);
    }
    .nav-links { display: flex; gap: 2rem; list-style: none; }
    .nav-links a {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 600; font-size: .85rem; letter-spacing: .1em;
      text-transform: uppercase; color: var(--text); opacity: .7;
      transition: opacity .2s, color .2s; position: relative;
    }
    .nav-links a::after {
      content: ''; position: absolute; bottom: -3px; left: 0; right: 100%;
      height: 2px; background: var(--accent); transition: right .25s;
    }
    .nav-links a:hover { opacity: 1; color: var(--accent); }
    .nav-links a:hover::after { right: 0; }
    .nav-actions { display: flex; align-items: center; gap: 1rem; }
    .btn-theme {
      background: none; border: 1px solid var(--border); color: var(--text);
      border-radius: var(--radius); width: 36px; height: 36px; cursor: pointer;
      display: grid; place-items: center; font-size: 1rem;
      transition: border-color .2s, background .2s;
    }
    .btn-theme:hover { border-color: var(--accent); background: var(--surface); }
    .btn-cta-nav {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 700; font-size: .85rem; letter-spacing: .1em; text-transform: uppercase;
      background: var(--accent); color: #000; border: none;
      padding: .55rem 1.2rem; border-radius: var(--radius); cursor: pointer;
      transition: transform .15s, box-shadow .15s;
    }
    .btn-cta-nav:hover { transform: translateY(-2px); box-shadow: 0 6px 20px color-mix(in srgb, var(--accent) 40%, transparent); }
    .hamburger {
      display: none; flex-direction: column; gap: 5px;
      background: none; border: none; cursor: pointer; padding: 4px;
    }
    .hamburger span {
      display: block; width: 24px; height: 2px;
      background: var(--text); border-radius: 2px; transition: transform .3s, opacity .3s;
    }
    .hamburger.open span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
    .hamburger.open span:nth-child(2) { opacity: 0; }
    .hamburger.open span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }
    .mobile-menu {
      display: none; position: fixed; top: 64px; left: 0; right: 0;
      background: var(--bg2); border-bottom: 1px solid var(--border);
      padding: 1.5rem 5vw; z-index: 99;
      flex-direction: column; gap: 1.2rem;
      transform: translateY(-10px); opacity: 0;
      transition: opacity .3s, transform .3s;
    }
    .mobile-menu.open { display: flex; opacity: 1; transform: translateY(0); }
    .mobile-menu a {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 600; font-size: 1.1rem; letter-spacing: .1em; text-transform: uppercase;
    }

    /* HERO */
    #hero {
      min-height: 100svh; display: grid; grid-template-columns: 1fr 1fr;
      position: relative; overflow: hidden;
    }
    .hero-left {
      padding: 10rem 5vw 6rem; display: flex;
      flex-direction: column; justify-content: flex-end;
      position: relative; z-index: 2;
    }
    .hero-tag {
      font-family: 'Barlow Condensed', sans-serif;
      font-size: .75rem; font-weight: 700; letter-spacing: .25em; text-transform: uppercase;
      color: var(--accent); margin-bottom: 1.2rem;
      display: flex; align-items: center; gap: .6rem;
      opacity: 0; animation: fadeUp .6s .2s forwards;
    }
    .hero-tag::before { content: ''; width: 2rem; height: 2px; background: var(--accent); }
    .hero-h1 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(4.5rem, 9vw, 9rem); line-height: .95; letter-spacing: .02em;
      opacity: 0; animation: fadeUp .7s .35s forwards;
    }
    .hero-h1 span { color: var(--accent); }
    .hero-sub {
      margin-top: 1.5rem; font-size: 1.05rem; line-height: 1.7;
      color: var(--muted); max-width: 40ch;
      opacity: 0; animation: fadeUp .7s .5s forwards;
    }
    .hero-btns {
      margin-top: 2.5rem; display: flex; gap: 1rem; flex-wrap: wrap;
      opacity: 0; animation: fadeUp .7s .65s forwards;
    }
    .btn-primary {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 700; font-size: .9rem; letter-spacing: .12em; text-transform: uppercase;
      background: var(--accent); color: #000; border: none;
      padding: .85rem 2rem; border-radius: var(--radius); cursor: pointer;
      transition: transform .15s, box-shadow .15s;
      display: inline-flex; align-items: center; gap: .5rem;
    }
    .btn-primary:hover { transform: translateY(-3px); box-shadow: 0 10px 30px color-mix(in srgb, var(--accent) 35%, transparent); }
    .btn-secondary {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 700; font-size: .9rem; letter-spacing: .12em; text-transform: uppercase;
      background: transparent; color: var(--text); border: 1px solid var(--border);
      padding: .85rem 2rem; border-radius: var(--radius); cursor: pointer;
      transition: border-color .2s, color .2s;
    }
    .btn-secondary:hover { border-color: var(--text); }
    .hero-stats {
      margin-top: 4rem; display: flex; gap: 2.5rem;
      opacity: 0; animation: fadeUp .7s .8s forwards;
    }
    .hero-stat-num {
      font-family: 'Bebas Neue', sans-serif;
      font-size: 2.4rem; letter-spacing: .04em; color: var(--accent); line-height: 1;
    }
    .hero-stat-label {
      font-size: .75rem; font-weight: 600; letter-spacing: .1em;
      text-transform: uppercase; color: var(--muted); margin-top: .2rem;
    }
    .hero-right { position: relative; overflow: hidden; }
    .hero-img-wrap { position: absolute; inset: 0; background: var(--bg3); overflow: hidden; }
    .hero-img-wrap::after {
      content: ''; position: absolute; inset: 0;
      background: linear-gradient(100deg, var(--bg) 0%, transparent 50%);
    }
    .hero-svg-bg {
      width: 100%; height: 100%;
      background:
        radial-gradient(ellipse at 70% 30%, color-mix(in srgb, var(--accent) 12%, transparent) 0%, transparent 60%),
        radial-gradient(ellipse at 30% 80%, color-mix(in srgb, var(--accent2) 8%, transparent) 0%, transparent 50%),
        var(--bg3);
      display: flex; align-items: center; justify-content: center;
      font-family: 'Bebas Neue', sans-serif;
      font-size: 18rem; color: var(--border); letter-spacing: -.05em; user-select: none;
    }
    .hero-badge {
      position: absolute; bottom: 3rem; right: 3rem;
      width: 110px; height: 110px; border: 2px solid var(--accent); border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      flex-direction: column; gap: .2rem; text-align: center;
      animation: rotSpin 20s linear infinite; z-index: 3;
    }
    @keyframes rotSpin { to { transform: rotate(360deg); } }
    .hero-badge-inner { animation: rotSpin 20s linear infinite reverse; }
    .hero-badge-text { font-family: 'Bebas Neue', sans-serif; font-size: 1rem; color: var(--accent); letter-spacing: .06em; }
    .hero-badge-sub { font-size: .6rem; font-weight: 600; letter-spacing: .15em; text-transform: uppercase; color: var(--muted); }
    .ticker-bar {
      position: absolute; bottom: 0; left: 0; right: 0;
      background: var(--accent); height: 40px;
      display: flex; align-items: center; overflow: hidden; z-index: 5;
    }
    .ticker-track { display: flex; white-space: nowrap; animation: tickMove 22s linear infinite; }
    @keyframes tickMove { from { transform: translateX(0); } to { transform: translateX(-50%); } }
    .ticker-item {
      font-family: 'Barlow Condensed', sans-serif;
      font-weight: 700; font-size: .8rem; letter-spacing: .15em;
      text-transform: uppercase; color: #000; padding: 0 2rem;
    }
    .ticker-dot { opacity: .4; }

    /* ABOUT */
    #about { padding: 8rem 5vw; display: grid; grid-template-columns: 1fr 1fr; gap: 6rem; align-items: center; }
    .about-grid-img { display: grid; grid-template-columns: 1fr 1fr; gap: .75rem; }
    .about-img-cell {
      border-radius: calc(var(--radius)*2); overflow: hidden; aspect-ratio: 3/4; background: var(--surface);
    }
    .about-img-cell:first-child { margin-top: 3rem; }
    .about-img-placeholder {
      width: 100%; height: 100%; background: var(--bg3);
      display: flex; align-items: center; justify-content: center;
    }
    .about-img-placeholder svg { width: 48px; height: 48px; opacity: .15; }
    .about-badge-founded {
      display: block; margin: 1.5rem auto 0;
      background: var(--accent); color: #000;
      font-family: 'Bebas Neue', sans-serif; font-size: 1.2rem; letter-spacing: .08em;
      padding: .6rem 1.6rem; border-radius: var(--radius); text-align: center;
    }
    .section-tag {
      font-family: 'Barlow Condensed', sans-serif;
      font-size: .7rem; font-weight: 700; letter-spacing: .3em; text-transform: uppercase;
      color: var(--accent); display: flex; align-items: center; gap: .6rem; margin-bottom: 1rem;
    }
    .section-tag::before { content: ''; width: 1.5rem; height: 2px; background: var(--accent); }
    .section-h2 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(2.8rem, 5vw, 4.5rem); line-height: 1; letter-spacing: .03em;
    }
    .section-h2 span { color: var(--accent); }
    .body-text { margin-top: 1.5rem; font-size: 1rem; line-height: 1.8; color: var(--muted); max-width: 52ch; }
    .about-pills { margin-top: 2rem; display: flex; flex-wrap: wrap; gap: .6rem; }
    .pill {
      font-family: 'Barlow Condensed', sans-serif; font-weight: 600; font-size: .78rem; letter-spacing: .1em;
      text-transform: uppercase; border: 1px solid var(--border); padding: .4rem .9rem;
      border-radius: 100px; color: var(--muted); transition: border-color .2s, color .2s;
    }
    .pill:hover { border-color: var(--accent); color: var(--accent); }

    /* SERVICES */
    #services { padding: 6rem 5vw; background: var(--bg2); }
    .services-header {
      display: flex; align-items: flex-end; justify-content: space-between;
      margin-bottom: 1.5rem; flex-wrap: wrap; gap: 1.5rem;
    }
    .search-wrap { position: relative; }
    .search-input {
      background: var(--surface); border: 1px solid var(--border); color: var(--text);
      font-family: 'Barlow', sans-serif; font-size: .9rem;
      padding: .7rem 1rem .7rem 2.6rem; border-radius: var(--radius); width: 260px;
      outline: none; transition: border-color .2s;
    }
    .search-input::placeholder { color: var(--muted); }
    .search-input:focus { border-color: var(--accent); }
    .search-icon { position: absolute; left: .8rem; top: 50%; transform: translateY(-50%); color: var(--muted); pointer-events: none; font-size: .95rem; }
    .filter-tabs { display: flex; gap: .5rem; flex-wrap: wrap; margin-bottom: 2rem; }
    .filter-tab {
      font-family: 'Barlow Condensed', sans-serif; font-weight: 700; font-size: .78rem;
      letter-spacing: .1em; text-transform: uppercase;
      background: var(--surface); color: var(--muted); border: 1px solid var(--border);
      padding: .45rem .9rem; border-radius: var(--radius); cursor: pointer; transition: all .2s;
    }
    .filter-tab.active, .filter-tab:hover { background: var(--accent); color: #000; border-color: var(--accent); }
    .services-grid {
      display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1.5rem;
    }
    .service-card {
      background: var(--surface); border: 1px solid var(--border);
      border-radius: calc(var(--radius)*2); padding: 2rem;
      position: relative; overflow: hidden;
      transition: transform .25s, box-shadow .25s, border-color .25s;
    }
    .service-card::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px;
      background: var(--accent); transform: scaleX(0); transform-origin: left; transition: transform .35s;
    }
    .service-card:hover { transform: translateY(-6px); box-shadow: 0 20px 50px rgba(0,0,0,.3); border-color: var(--accent); }
    .service-card:hover::before { transform: scaleX(1); }
    .service-card[data-hidden="true"] { display: none; }
    .service-icon {
      width: 52px; height: 52px;
      background: color-mix(in srgb, var(--accent) 12%, transparent);
      border: 1px solid color-mix(in srgb, var(--accent) 25%, transparent);
      border-radius: var(--radius); display: grid; place-items: center;
      font-size: 1.5rem; margin-bottom: 1.3rem;
    }
    .service-name { font-family: 'Barlow Condensed', sans-serif; font-weight: 700; font-size: 1.3rem; letter-spacing: .05em; text-transform: uppercase; }
    .service-desc { margin-top: .6rem; font-size: .9rem; line-height: 1.7; color: var(--muted); }
    .service-meta { margin-top: 1.2rem; display: flex; align-items: center; gap: .5rem; font-size: .75rem; font-weight: 600; letter-spacing: .1em; text-transform: uppercase; color: var(--accent); }
    .no-results { grid-column: 1/-1; text-align: center; padding: 4rem 0; color: var(--muted); font-family: 'Barlow Condensed', sans-serif; font-size: 1.2rem; letter-spacing: .1em; text-transform: uppercase; display: none; }

    /* PLANS */
    #plans { padding: 8rem 5vw; }
    .plans-header { text-align: center; margin-bottom: 3.5rem; }
    .plans-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; max-width: 1100px; margin: 0 auto; }
    .plan-card {
      border: 1px solid var(--border); border-radius: calc(var(--radius)*2);
      padding: 2.5rem 2rem; position: relative; overflow: hidden;
      transition: transform .25s; background: var(--surface);
    }
    .plan-card.featured { border-color: var(--accent); background: color-mix(in srgb, var(--accent) 5%, var(--surface)); }
    .plan-card.featured .plan-price-num { color: var(--accent); }
    .plan-badge {
      position: absolute; top: 1.2rem; right: 1.2rem;
      background: var(--accent); color: #000;
      font-family: 'Barlow Condensed', sans-serif; font-weight: 700; font-size: .7rem; letter-spacing: .12em; text-transform: uppercase;
      padding: .3rem .7rem; border-radius: 100px;
    }
    .plan-name { font-family: 'Bebas Neue', sans-serif; font-size: 1.8rem; letter-spacing: .06em; }
    .plan-price { margin-top: 1.2rem; display: flex; align-items: baseline; gap: .3rem; }
    .plan-price-curr { font-size: 1.1rem; font-weight: 600; color: var(--muted); }
    .plan-price-num { font-family: 'Bebas Neue', sans-serif; font-size: 3.5rem; line-height: 1; }
    .plan-price-per { font-size: .8rem; color: var(--muted); }
    .plan-desc { font-size: .9rem; color: var(--muted); margin-top: .6rem; line-height: 1.6; }
    .plan-divider { height: 1px; background: var(--border); margin: 1.5rem 0; }
    .plan-features { list-style: none; display: flex; flex-direction: column; gap: .75rem; }
    .plan-features li { display: flex; align-items: center; gap: .7rem; font-size: .9rem; color: var(--muted); }
    .plan-features li::before { content: '✓'; color: var(--accent); font-weight: 700; flex-shrink: 0; }
    .plan-features li.na { text-decoration: line-through; opacity: .4; }
    .plan-features li.na::before { content: '✗'; color: var(--muted); }
    .btn-plan {
      margin-top: 2rem; width: 100%;
      font-family: 'Barlow Condensed', sans-serif; font-weight: 700; font-size: .9rem; letter-spacing: .1em; text-transform: uppercase;
      background: transparent; color: var(--text); border: 1px solid var(--border);
      padding: .85rem; border-radius: var(--radius); cursor: pointer; transition: all .2s;
    }
    .plan-card.featured .btn-plan { background: var(--accent); color: #000; border-color: var(--accent); }
    .btn-plan:hover { border-color: var(--accent); color: var(--accent); }
    .plan-card.featured .btn-plan:hover { opacity: .85; color: #000; }

    /* TRAINERS */
    #trainers { padding: 8rem 5vw; background: var(--bg2); }
    .trainers-header { margin-bottom: 3rem; }
    .trainers-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap: 1.5rem; }
    .trainer-card { border-radius: calc(var(--radius)*2); overflow: hidden; position: relative; aspect-ratio: 3/4; background: var(--surface); cursor: pointer; }
    .trainer-photo { width: 100%; height: 100%; background: var(--bg3); display: grid; place-items: center; font-size: 5rem; color: var(--border); user-select: none; }
    .trainer-overlay {
      position: absolute; inset: 0;
      background: linear-gradient(to top, rgba(0,0,0,.92) 40%, transparent);
      display: flex; flex-direction: column; justify-content: flex-end; padding: 1.5rem;
      transform: translateY(8px); opacity: .9; transition: transform .3s, opacity .3s;
    }
    .trainer-card:hover .trainer-overlay { transform: translateY(0); opacity: 1; }
    .trainer-name { font-family: 'Bebas Neue', sans-serif; font-size: 1.5rem; letter-spacing: .05em; }
    .trainer-role { font-size: .75rem; font-weight: 600; letter-spacing: .12em; text-transform: uppercase; color: var(--accent); margin-top: .1rem; }
    .trainer-tags { margin-top: .75rem; display: flex; flex-wrap: wrap; gap: .4rem; }
    .trainer-tag { font-size: .68rem; font-weight: 600; letter-spacing: .08em; text-transform: uppercase; background: rgba(255,255,255,.1); padding: .25rem .6rem; border-radius: 100px; color: rgba(255,255,255,.7); }
    .trainer-social { display: flex; gap: .5rem; margin-top: .75rem; opacity: 0; transform: translateY(6px); transition: opacity .3s .1s, transform .3s .1s; }
    .trainer-card:hover .trainer-social { opacity: 1; transform: translateY(0); }
    .soc-btn { width: 30px; height: 30px; background: rgba(255,255,255,.12); border-radius: 50%; display: grid; place-items: center; font-size: .7rem; color: #fff; transition: background .2s; }
    .soc-btn:hover { background: var(--accent); color: #000; }

    /* CONTACT */
    #contact { padding: 8rem 5vw; display: grid; grid-template-columns: 1fr 1.2fr; gap: 6rem; align-items: start; }
    .contact-detail { margin-top: 2.5rem; display: flex; flex-direction: column; gap: 1.5rem; }
    .contact-item { display: flex; gap: 1rem; align-items: flex-start; }
    .contact-item-icon {
      width: 42px; height: 42px; flex-shrink: 0;
      background: color-mix(in srgb, var(--accent) 10%, transparent);
      border: 1px solid color-mix(in srgb, var(--accent) 20%, transparent);
      border-radius: var(--radius); display: grid; place-items: center; font-size: 1rem;
    }
    .contact-item-label { font-size: .7rem; font-weight: 700; letter-spacing: .15em; text-transform: uppercase; color: var(--muted); }
    .contact-item-val { font-size: 1rem; font-weight: 500; margin-top: .2rem; }
    .contact-hours { margin-top: 2.5rem; border: 1px solid var(--border); border-radius: calc(var(--radius)*2); overflow: hidden; }
    .hours-title { background: var(--surface); padding: .8rem 1.2rem; font-family: 'Barlow Condensed', sans-serif; font-weight: 700; font-size: .85rem; letter-spacing: .1em; text-transform: uppercase; color: var(--accent); }
    .hours-row { display: flex; justify-content: space-between; padding: .65rem 1.2rem; font-size: .88rem; border-top: 1px solid var(--border); color: var(--muted); }
    .hours-row span:last-child { color: var(--text); font-weight: 500; }

    /* FORM */
    .form-card { background: var(--surface); border: 1px solid var(--border); border-radius: calc(var(--radius)*2); padding: 2.5rem; }
    .form-title { font-family: 'Bebas Neue', sans-serif; font-size: 2.2rem; letter-spacing: .04em; margin-bottom: .4rem; }
    .form-sub { font-size: .9rem; color: var(--muted); margin-bottom: 2rem; }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
    .form-group { display: flex; flex-direction: column; gap: .5rem; margin-bottom: 1.2rem; }
    .form-label { font-size: .72rem; font-weight: 700; letter-spacing: .12em; text-transform: uppercase; color: var(--muted); }
    .form-input, .form-textarea, .form-select {
      background: var(--bg2); border: 1px solid var(--border); color: var(--text);
      font-family: 'Barlow', sans-serif; font-size: .95rem; padding: .85rem 1rem;
      border-radius: var(--radius); outline: none; width: 100%;
      transition: border-color .2s, box-shadow .2s;
    }
    .form-input::placeholder, .form-textarea::placeholder { color: var(--muted); }
    .form-input:focus, .form-textarea:focus, .form-select:focus { border-color: var(--accent); box-shadow: 0 0 0 3px color-mix(in srgb, var(--accent) 12%, transparent); }
    .form-input.error, .form-textarea.error, .form-select.error { border-color: var(--accent2); }
    .form-error-msg { font-size: .76rem; color: var(--accent2); display: none; margin-top: -.3rem; }
    .form-error-msg.show { display: block; }
    .form-textarea { resize: vertical; min-height: 130px; }
    .form-select { appearance: none; cursor: pointer; }
    .form-checkbox-row { display: flex; align-items: flex-start; gap: .75rem; margin-bottom: 1.5rem; }
    .form-checkbox { width: 18px; height: 18px; flex-shrink: 0; margin-top: 2px; accent-color: var(--accent); cursor: pointer; }
    .form-checkbox-label { font-size: .85rem; color: var(--muted); line-height: 1.5; }
    .btn-submit {
      font-family: 'Barlow Condensed', sans-serif; font-weight: 700; font-size: 1rem; letter-spacing: .12em; text-transform: uppercase;
      background: var(--accent); color: #000; border: none; padding: 1rem 2.5rem;
      border-radius: var(--radius); cursor: pointer;
      display: inline-flex; align-items: center; gap: .7rem; justify-content: center;
      transition: transform .15s, box-shadow .15s, opacity .2s; width: 100%;
    }
    .btn-submit:hover:not(:disabled) { transform: translateY(-2px); box-shadow: 0 10px 30px color-mix(in srgb, var(--accent) 35%, transparent); }
    .btn-submit:disabled { opacity: .6; cursor: not-allowed; }
    .spinner { width: 18px; height: 18px; border-radius: 50%; border: 2px solid #000; border-top-color: transparent; animation: spinFast .6s linear infinite; display: none; }
    @keyframes spinFast { to { transform: rotate(360deg); } }
    .btn-submit.loading .spinner { display: block; }
    .btn-submit.loading .btn-text { display: none; }
    .form-success { display: none; text-align: center; padding: 3rem 2rem; flex-direction: column; align-items: center; gap: 1rem; }
    .form-success.show { display: flex; }
    .form-success-icon { width: 64px; height: 64px; background: color-mix(in srgb, var(--accent) 15%, transparent); border-radius: 50%; display: grid; place-items: center; font-size: 2rem; animation: popIn .5s cubic-bezier(.175,.885,.32,1.275); }
    @keyframes popIn { from { transform: scale(0); opacity: 0; } to { transform: scale(1); opacity: 1; } }
    .form-success-title { font-family: 'Bebas Neue', sans-serif; font-size: 2rem; letter-spacing: .05em; color: var(--accent); }
    .form-success-body { font-size: .9rem; color: var(--muted); max-width: 30ch; line-height: 1.6; }
    .form-success-back { margin-top: .5rem; font-family: 'Barlow Condensed', sans-serif; font-weight: 700; font-size: .85rem; letter-spacing: .1em; text-transform: uppercase; color: var(--accent); background: none; border: 1px solid var(--accent); padding: .55rem 1.4rem; border-radius: var(--radius); cursor: pointer; }

    /* FOOTER */
    footer { background: var(--bg3); border-top: 1px solid var(--border); padding: 4rem 5vw 2rem; }
    .footer-top { display: grid; grid-template-columns: 1.5fr 1fr 1fr 1fr; gap: 3rem; padding-bottom: 3rem; border-bottom: 1px solid var(--border); }
    .footer-brand-desc { font-size: .9rem; color: var(--muted); line-height: 1.7; margin-top: .8rem; max-width: 30ch; }
    .footer-col-title { font-family: 'Barlow Condensed', sans-serif; font-weight: 700; font-size: .78rem; letter-spacing: .15em; text-transform: uppercase; color: var(--text); margin-bottom: 1.2rem; }
    .footer-links { list-style: none; display: flex; flex-direction: column; gap: .7rem; }
    .footer-links a { font-size: .88rem; color: var(--muted); transition: color .2s; }
    .footer-links a:hover { color: var(--accent); }
    .footer-bottom { display: flex; align-items: center; justify-content: space-between; padding-top: 1.5rem; flex-wrap: wrap; gap: 1rem; }
    .footer-copy { font-size: .8rem; color: var(--muted); }
    .footer-socials { display: flex; gap: .75rem; }
    .footer-soc { width: 34px; height: 34px; border: 1px solid var(--border); border-radius: var(--radius); display: grid; place-items: center; font-size: .85rem; color: var(--muted); transition: border-color .2s, color .2s; }
    .footer-soc:hover { border-color: var(--accent); color: var(--accent); }

    /* REVEAL */
    .reveal { opacity: 0; transform: translateY(30px); transition: opacity .7s, transform .7s; }
    .reveal.visible { opacity: 1; transform: translateY(0); }

    @keyframes fadeUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

    /* RESPONSIVE */
    @media (max-width: 1024px) {
      #hero { grid-template-columns: 1fr; }
      .hero-right { display: none; }
      .hero-left { padding: 9rem 5vw 6rem; }
      #about { grid-template-columns: 1fr; gap: 3rem; }
      .about-visual { order: -1; }
      #contact { grid-template-columns: 1fr; gap: 3rem; }
      .plans-grid { grid-template-columns: 1fr; max-width: 420px; }
      .footer-top { grid-template-columns: 1fr 1fr; gap: 2.5rem; }
    }
    @media (max-width: 768px) {
      .nav-links, .btn-cta-nav { display: none; }
      .hamburger { display: flex; }
      .form-row { grid-template-columns: 1fr; }
      .footer-top { grid-template-columns: 1fr; }
      .hero-stats { flex-wrap: wrap; gap: 1.5rem; }
    }
  </style>
</head>
<body>

<nav id="mainNav">
  <a href="#hero" class="nav-logo">IRONFORGE</a>
  <ul class="nav-links">
    <li><a href="#about">About</a></li>
    <li><a href="#services">Services</a></li>
    <li><a href="#plans">Plans</a></li>
    <li><a href="#trainers">Trainers</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <div class="nav-actions">
    <button class="btn-theme" id="themeToggle" title="Toggle dark/light mode">🌙</button>
    <button class="btn-cta-nav" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Join Now</button>
    <button class="hamburger" id="hamburger" aria-label="Toggle menu">
      <span></span><span></span><span></span>
    </button>
  </div>
</nav>

<div class="mobile-menu" id="mobileMenu">
  <a href="#about"    onclick="closeMobileMenu()">About</a>
  <a href="#services" onclick="closeMobileMenu()">Services</a>
  <a href="#plans"    onclick="closeMobileMenu()">Plans</a>
  <a href="#trainers" onclick="closeMobileMenu()">Trainers</a>
  <a href="#contact"  onclick="closeMobileMenu()">Contact</a>
</div>

<!-- HERO -->
<section id="hero">
  <div class="hero-left">
    <div class="hero-tag">Est. 2019 — Negombo, Sri Lanka</div>
    <h1 class="hero-h1">FORGE<br>YOUR<br><span>LIMITS</span></h1>
    <p class="hero-sub">World-class equipment, elite coaching, and a community that pushes you beyond what you thought possible.</p>
    <div class="hero-btns">
      <button class="btn-primary" onclick="document.getElementById('plans').scrollIntoView({behavior:'smooth'})">View Plans →</button>
      <button class="btn-secondary" onclick="document.getElementById('about').scrollIntoView({behavior:'smooth'})">Discover More</button>
    </div>
    <div class="hero-stats">
      <div><div class="hero-stat-num" data-count="2400">0</div><div class="hero-stat-label">Active Members</div></div>
      <div><div class="hero-stat-num" data-count="18">0</div><div class="hero-stat-label">Expert Trainers</div></div>
      <div><div class="hero-stat-num" data-count="42">0</div><div class="hero-stat-label">Weekly Classes</div></div>
    </div>
  </div>
  <div class="hero-right">
    <div class="hero-img-wrap">
      <div class="hero-svg-bg">IF</div>
    </div>
    <div class="hero-badge">
      <div class="hero-badge-inner">
        <div class="hero-badge-text">IRONFORGE</div>
        <div class="hero-badge-sub">Est. 2019</div>
      </div>
    </div>
  </div>
  <div class="ticker-bar">
    <div class="ticker-track" id="ticker"></div>
  </div>
</section>

<!-- ABOUT -->
<section id="about">
  <div class="about-visual reveal">
    <div class="about-grid-img">
      <div class="about-img-cell">
        <div class="about-img-placeholder">
          <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
            <rect x="8" y="20" width="4" height="8" rx="2" fill="currentColor"/>
            <rect x="12" y="16" width="24" height="16" rx="3" fill="currentColor"/>
            <rect x="36" y="20" width="4" height="8" rx="2" fill="currentColor"/>
            <rect x="16" y="18" width="16" height="12" rx="2" fill="var(--bg)"/>
          </svg>
        </div>
      </div>
      <div class="about-img-cell">
        <div class="about-img-placeholder">
          <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="24" cy="14" r="6" fill="currentColor"/>
            <path d="M10 38c0-7.732 6.268-14 14-14s14 6.268 14 14" stroke="currentColor" stroke-width="3" fill="none"/>
          </svg>
        </div>
      </div>
    </div>
    <div class="about-badge-founded">Founded 2019</div>
  </div>
  <div class="about-content reveal">
    <div class="section-tag">Our Story</div>
    <h2 class="section-h2">MORE THAN A<br><span>GYM</span></h2>
    <p class="body-text">IronForge was built on a simple belief: everyone deserves access to a space that challenges them, supports them, and transforms them. From cutting-edge equipment to nationally certified coaches, every detail is crafted to help you perform at your absolute best.</p>
    <p class="body-text" style="margin-top:1rem;">Whether you're just starting out or training for elite competition, we meet you where you are — and take you further than you've ever gone.</p>
    <div class="about-pills">
      <span class="pill">HIIT Training</span>
      <span class="pill">Powerlifting</span>
      <span class="pill">Yoga & Mindfulness</span>
      <span class="pill">Nutrition Coaching</span>
      <span class="pill">Recovery & Spa</span>
      <span class="pill">Kids Fitness</span>
    </div>
  </div>
</section>

<!-- SERVICES -->
<section id="services">
  <div class="services-header">
    <div>
      <div class="section-tag">What We Offer</div>
      <h2 class="section-h2">OUR <span>SERVICES</span></h2>
    </div>
    <div class="search-wrap">
      <span class="search-icon">🔍</span>
      <input type="text" class="search-input" id="serviceSearch" placeholder="Search services…" autocomplete="off">
    </div>
  </div>
  <div class="filter-tabs" id="filterTabs">
    <button class="filter-tab active" data-filter="all">All</button>
    <button class="filter-tab" data-filter="strength">Strength</button>
    <button class="filter-tab" data-filter="cardio">Cardio</button>
    <button class="filter-tab" data-filter="wellness">Wellness</button>
    <button class="filter-tab" data-filter="coaching">Coaching</button>
  </div>
  <div class="services-grid" id="servicesGrid"></div>
  <div class="no-results" id="noResults">No services found</div>
</section>

<!-- PLANS -->
<section id="plans">
  <div class="plans-header reveal">
    <div class="section-tag" style="justify-content:center;">Membership</div>
    <h2 class="section-h2" style="text-align:center;">CHOOSE YOUR <span>PLAN</span></h2>
  </div>
  <div class="plans-grid">
    <div class="plan-card reveal">
      <div class="plan-name">Starter</div>
      <div class="plan-price"><span class="plan-price-curr">$</span><span class="plan-price-num">29</span><span class="plan-price-per">/ mo</span></div>
      <p class="plan-desc">Perfect for beginners building their foundation.</p>
      <div class="plan-divider"></div>
      <ul class="plan-features">
        <li>Gym floor access (6am–10pm)</li>
        <li>4 group classes/month</li>
        <li>Locker & shower access</li>
        <li class="na">Personal trainer sessions</li>
        <li class="na">Nutrition consultations</li>
        <li class="na">Recovery & spa access</li>
      </ul>
      <button class="btn-plan" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Started</button>
    </div>
    <div class="plan-card featured reveal">
      <div class="plan-badge">Most Popular</div>
      <div class="plan-name">Pro</div>
      <div class="plan-price"><span class="plan-price-curr">$</span><span class="plan-price-num">59</span><span class="plan-price-per">/ mo</span></div>
      <p class="plan-desc">For serious athletes who want real, fast results.</p>
      <div class="plan-divider"></div>
      <ul class="plan-features">
        <li>24/7 gym access</li>
        <li>Unlimited group classes</li>
        <li>Locker & shower access</li>
        <li>2 PT sessions/month</li>
        <li>Monthly nutrition check-in</li>
        <li class="na">Recovery & spa access</li>
      </ul>
      <button class="btn-plan" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Started</button>
    </div>
    <div class="plan-card reveal">
      <div class="plan-name">Elite</div>
      <div class="plan-price"><span class="plan-price-curr">$</span><span class="plan-price-num">99</span><span class="plan-price-per">/ mo</span></div>
      <p class="plan-desc">The complete experience — zero compromises.</p>
      <div class="plan-divider"></div>
      <ul class="plan-features">
        <li>24/7 gym access</li>
        <li>Unlimited group classes</li>
        <li>Locker & shower access</li>
        <li>4 PT sessions/month</li>
        <li>Weekly nutrition plan</li>
        <li>Full recovery & spa access</li>
      </ul>
      <button class="btn-plan" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'})">Get Started</button>
    </div>
  </div>
</section>

<!-- TRAINERS -->
<section id="trainers">
  <div class="trainers-header reveal">
    <div class="section-tag">The Team</div>
    <h2 class="section-h2">MEET YOUR <span>TRAINERS</span></h2>
  </div>
  <div class="trainers-grid" id="trainersGrid"></div>
</section>

<!-- CONTACT -->
<section id="contact">
  <div class="contact-info reveal">
    <div class="section-tag">Get In Touch</div>
    <h2 class="section-h2">READY TO <span>START?</span></h2>
    <p class="body-text">Fill out the form and one of our team will get back to you within 24 hours to discuss your fitness goals.</p>
    <div class="contact-detail">
      <div class="contact-item">
        <div class="contact-item-icon">📍</div>
        <div><div class="contact-item-label">Location</div><div class="contact-item-val">42 Marine Drive, Negombo 11500, Sri Lanka</div></div>
      </div>
      <div class="contact-item">
        <div class="contact-item-icon">📞</div>
        <div><div class="contact-item-label">Phone</div><div class="contact-item-val">+94 31 223 4567</div></div>
      </div>
      <div class="contact-item">
        <div class="contact-item-icon">✉️</div>
        <div><div class="contact-item-label">Email</div><div class="contact-item-val">hello@ironforge.lk</div></div>
      </div>
    </div>
    <div class="contact-hours">
      <div class="hours-title">Opening Hours</div>
      <div class="hours-row"><span>Mon – Fri</span><span>5:00 AM – 11:00 PM</span></div>
      <div class="hours-row"><span>Saturday</span><span>6:00 AM – 10:00 PM</span></div>
      <div class="hours-row"><span>Sunday</span><span>7:00 AM – 8:00 PM</span></div>
      <div class="hours-row"><span>Public Holidays</span><span>8:00 AM – 6:00 PM</span></div>
    </div>
  </div>

  <div class="form-card reveal">
    <div id="formWrap">
      <div class="form-title">Send a Message</div>
      <p class="form-sub">We'd love to hear from you. Fields marked * are required.</p>
      <form id="contactForm" novalidate>
        <div class="form-row">
          <div class="form-group">
            <label class="form-label" for="fname">First Name *</label>
            <input type="text" id="fname" class="form-input" placeholder="Alex" autocomplete="given-name">
            <span class="form-error-msg" id="fnameErr">Please enter your first name.</span>
          </div>
          <div class="form-group">
            <label class="form-label" for="lname">Last Name</label>
            <input type="text" id="lname" class="form-input" placeholder="Johnson" autocomplete="family-name">
          </div>
        </div>
        <div class="form-group">
          <label class="form-label" for="email">Email Address *</label>
          <input type="email" id="email" class="form-input" placeholder="alex@example.com" autocomplete="email">
          <span class="form-error-msg" id="emailErr">Please enter a valid email address.</span>
        </div>
        <div class="form-group">
          <label class="form-label" for="phone">Phone Number</label>
          <input type="tel" id="phone" class="form-input" placeholder="+94 71 234 5678" autocomplete="tel">
        </div>
        <div class="form-group">
          <label class="form-label" for="interest">I'm interested in *</label>
          <select id="interest" class="form-select form-input">
            <option value="" disabled selected>Select an option…</option>
            <option value="starter">Starter Membership</option>
            <option value="pro">Pro Membership</option>
            <option value="elite">Elite Membership</option>
            <option value="personal">Personal Training</option>
            <option value="classes">Group Classes</option>
            <option value="other">Just Browsing</option>
          </select>
          <span class="form-error-msg" id="interestErr">Please select an option.</span>
        </div>
        <div class="form-group">
          <label class="form-label" for="message">Message *</label>
          <textarea id="message" class="form-textarea" placeholder="Tell us about your fitness goals…"></textarea>
          <span class="form-error-msg" id="messageErr">Please write a message (min. 10 characters).</span>
        </div>
        <div class="form-checkbox-row">
          <input type="checkbox" id="consent" class="form-checkbox">
          <label for="consent" class="form-checkbox-label">I agree to receive updates about IronForge programs and offers. Unsubscribe anytime.</label>
        </div>
        <button type="submit" class="btn-submit" id="submitBtn">
          <span class="btn-text">Send Message</span>
          <div class="spinner"></div>
        </button>
      </form>
    </div>
    <div class="form-success" id="formSuccess">
      <div class="form-success-icon">✓</div>
      <div class="form-success-title">Message Sent!</div>
      <p class="form-success-body">Thanks for reaching out. We'll get back to you within 24 hours.</p>
      <button class="form-success-back" onclick="resetForm()">Send Another</button>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-top">
    <div>
      <div class="nav-logo">IRONFORGE</div>
      <p class="footer-brand-desc">Building stronger bodies and stronger communities since 2019.</p>
    </div>
    <div>
      <div class="footer-col-title">Navigate</div>
      <ul class="footer-links">
        <li><a href="#hero">Home</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#services">Services</a></li>
        <li><a href="#plans">Plans</a></li>
        <li><a href="#trainers">Trainers</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-col-title">Services</div>
      <ul class="footer-links">
        <li><a href="#services">Strength Training</a></li>
        <li><a href="#services">Cardio</a></li>
        <li><a href="#services">Yoga</a></li>
        <li><a href="#services">HIIT</a></li>
        <li><a href="#services">Nutrition</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-col-title">Legal</div>
      <ul class="footer-links">
        <li><a href="#">Privacy Policy</a></li>
        <li><a href="#">Terms of Use</a></li>
        <li><a href="#">Cookie Settings</a></li>
        <li><a href="#">Accessibility</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <div class="footer-copy">© 2024 IronForge Gym. All rights reserved.</div>
    <div class="footer-socials">
      <a href="#" class="footer-soc" aria-label="Instagram">IG</a>
      <a href="#" class="footer-soc" aria-label="Facebook">FB</a>
      <a href="#" class="footer-soc" aria-label="Twitter/X">𝕏</a>
      <a href="#" class="footer-soc" aria-label="YouTube">YT</a>
    </div>
  </div>
</footer>

<script>
const SERVICES = [
  { icon:'🏋️', name:'Powerlifting', desc:'Olympic platforms, squat racks, and expert coaching for maximal strength development.', cat:'strength', meta:'6 platforms available' },
  { icon:'🥊', name:'Boxing & Combat', desc:'Heavy bags, speed bags, and certified instructors covering boxing, Muay Thai and MMA.', cat:'cardio', meta:'Daily classes' },
  { icon:'🧘', name:'Yoga & Pilates', desc:'Dedicated studio space with expert-led sessions to build flexibility, balance and mindfulness.', cat:'wellness', meta:'Morning & evening' },
  { icon:'⚡', name:'HIIT Training', desc:'High-intensity interval classes that torch calories and build endurance in 45-minute bursts.', cat:'cardio', meta:'10 classes/week' },
  { icon:'🚴', name:'Cycling Studio', desc:'State-of-the-art spin bikes with immersive audiovisual classes and live performance tracking.', cat:'cardio', meta:'30-bike studio' },
  { icon:'🥗', name:'Nutrition Coaching', desc:'One-on-one consultations and personalised meal plans tailored to your specific fitness goals.', cat:'coaching', meta:'Certified dietitians' },
  { icon:'💆', name:'Recovery & Spa', desc:'Ice baths, saunas, and sports massage therapy to accelerate recovery and reduce injury risk.', cat:'wellness', meta:'Open daily' },
  { icon:'🎯', name:'Personal Training', desc:'Dedicated 1-on-1 sessions with certified trainers crafting a programme built around you.', cat:'coaching', meta:'Flexible scheduling' },
  { icon:'🏃', name:'Functional Fitness', desc:'Cross-training zones with kettlebells, sleds, battle ropes and suspension trainers.', cat:'strength', meta:'Open access' },
];

const TRAINERS = [
  { emoji:'💪', name:'Marcus Cole', role:'Head of Strength', tags:['Powerlifting','Olympic Lifting'] },
  { emoji:'🥊', name:'Aisha Nkomo', role:'Combat Coach', tags:['Boxing','Muay Thai'] },
  { emoji:'🧘', name:'Priya Sharma', role:'Wellness Lead', tags:['Yoga','Pilates','Mindfulness'] },
  { emoji:'⚡', name:'Jake Rivera', role:'HIIT Specialist', tags:['HIIT','Endurance','Cardio'] },
];

const TICKER_ITEMS = ['Free 7-Day Trial','No Joining Fee This Month','42 Weekly Classes','18 Expert Trainers','Open 24/7 for Pro & Elite Members','Sri Lanka\'s #1 Rated Gym','Book a Free Tour Today'];

// Build ticker
(function() {
  const t = document.getElementById('ticker');
  const items = [...TICKER_ITEMS,...TICKER_ITEMS];
  t.innerHTML = items.map(i => `<span class="ticker-item">${i}</span><span class="ticker-item ticker-dot">•</span>`).join('');
})();

// Build services
(function() {
  document.getElementById('servicesGrid').innerHTML = SERVICES.map(s =>
    `<div class="service-card reveal" data-cat="${s.cat}" data-name="${s.name.toLowerCase()} ${s.desc.toLowerCase()}">
      <div class="service-icon">${s.icon}</div>
      <div class="service-name">${s.name}</div>
      <p class="service-desc">${s.desc}</p>
      <div class="service-meta">→ ${s.meta}</div>
    </div>`
  ).join('');
  initReveal();
})();

// Build trainers
(function() {
  document.getElementById('trainersGrid').innerHTML = TRAINERS.map(t =>
    `<div class="trainer-card reveal">
      <div class="trainer-photo">${t.emoji}</div>
      <div class="trainer-overlay">
        <div class="trainer-name">${t.name}</div>
        <div class="trainer-role">${t.role}</div>
        <div class="trainer-tags">${t.tags.map(tg=>`<span class="trainer-tag">${tg}</span>`).join('')}</div>
        <div class="trainer-social">
          <a href="#" class="soc-btn" aria-label="Instagram">IG</a>
          <a href="#" class="soc-btn" aria-label="LinkedIn">in</a>
        </div>
      </div>
    </div>`
  ).join('');
  initReveal();
})();

// Filter + Search
let activeFilter = 'all';
document.getElementById('filterTabs').addEventListener('click', e => {
  const tab = e.target.closest('.filter-tab');
  if (!tab) return;
  document.querySelectorAll('.filter-tab').forEach(t => t.classList.remove('active'));
  tab.classList.add('active');
  activeFilter = tab.dataset.filter;
  applyFilters();
});
document.getElementById('serviceSearch').addEventListener('input', applyFilters);
function applyFilters() {
  const q = document.getElementById('serviceSearch').value.toLowerCase().trim();
  const cards = document.querySelectorAll('.service-card');
  let n = 0;
  cards.forEach(c => {
    const show = (activeFilter === 'all' || c.dataset.cat === activeFilter) && (!q || c.dataset.name.includes(q));
    c.dataset.hidden = show ? 'false' : 'true';
    if (show) n++;
  });
  document.getElementById('noResults').style.display = n === 0 ? 'block' : 'none';
}

// Nav scroll
window.addEventListener('scroll', () => {
  document.getElementById('mainNav').classList.toggle('scrolled', window.scrollY > 40);
}, { passive: true });

// Theme
document.getElementById('themeToggle').addEventListener('click', () => {
  const html = document.documentElement;
  const dark = html.getAttribute('data-theme') === 'dark';
  html.setAttribute('data-theme', dark ? 'light' : 'dark');
  document.getElementById('themeToggle').textContent = dark ? '🌙' : '☀️';
});

// Hamburger
const ham = document.getElementById('hamburger');
const mob = document.getElementById('mobileMenu');
ham.addEventListener('click', () => { ham.classList.toggle('open'); mob.classList.toggle('open'); });
function closeMobileMenu() { ham.classList.remove('open'); mob.classList.remove('open'); }

// Scroll Reveal
function initReveal() {
  const obs = new IntersectionObserver(entries => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); obs.unobserve(e.target); } });
  }, { threshold: 0.12 });
  document.querySelectorAll('.reveal:not(.visible)').forEach(el => obs.observe(el));
}
initReveal();

// Count-up
document.querySelectorAll('[data-count]').forEach(el => {
  new IntersectionObserver(([e]) => {
    if (!e.isIntersecting) return;
    const target = +el.dataset.count, dur = 1800, s = performance.now();
    (function step(now) {
      const p = Math.min((now-s)/dur,1), ease = 1-Math.pow(1-p,3);
      el.textContent = Math.round(ease*target).toLocaleString()+(target>=100?'+':'');
      if(p<1) requestAnimationFrame(step);
    })(s);
  }, {threshold:.5}).observe(el);
});

// Form
const form = document.getElementById('contactForm');
function validateField(id, errId, fn) {
  const el = document.getElementById(id);
  const err = document.getElementById(errId);
  const ok = fn(el.value.trim());
  el.classList.toggle('error', !ok);
  err.classList.toggle('show', !ok);
  return ok;
}
const validators = {
  fname: v => v.length >= 2,
  email: v => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v),
  interest: v => v !== '',
  message: v => v.length >= 10,
};
const errIds = { fname:'fnameErr', email:'emailErr', interest:'interestErr', message:'messageErr' };
['fname','email','interest','message'].forEach(id => {
  document.getElementById(id).addEventListener('blur', () => validateField(id, errIds[id], validators[id]));
});
form.addEventListener('submit', async e => {
  e.preventDefault();
  const ok = ['fname','email','interest','message'].map(id => validateField(id, errIds[id], validators[id])).every(Boolean);
  if (!ok) return;
  const btn = document.getElementById('submitBtn');
  btn.classList.add('loading'); btn.disabled = true;
  await new Promise(r => setTimeout(r, 1800));
  btn.classList.remove('loading'); btn.disabled = false;
  document.getElementById('formWrap').style.display = 'none';
  document.getElementById('formSuccess').classList.add('show');
});
function resetForm() {
  form.reset();
  document.querySelectorAll('.form-input,.form-textarea,.form-select').forEach(el => el.classList.remove('error'));
  document.querySelectorAll('.form-error-msg').forEach(el => el.classList.remove('show'));
  document.getElementById('formWrap').style.display = 'block';
  document.getElementById('formSuccess').classList.remove('show');
}
</script>
</body>
</html>